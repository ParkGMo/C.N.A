export function convertCodeISO2(countryName) {
  switch (countryName) {
    case '가나':
      return 'GH';

    default:
      break;
  }
}

export function convertCodeISO3(countryName) {
  switch (countryName) {
    case '가나':
      return 'GHA';

    default:
      break;
  }
}
